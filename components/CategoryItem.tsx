import { View, Text } from 'react-native'
import React from 'react'

type Props = {
    imageUri: string,
    label: string,
}

export default function CategoryItem({imageUri, label}:Props) {
  return <></>
}